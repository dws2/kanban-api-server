export class Item {
  constructor(id, title, description, dueDate, list) {
    this.id = id
    this.title = title
    this.description = description
    this.dueDate = dueDate
    this.list = list
  }
}